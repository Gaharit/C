#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <string.h>
void main()
{
	char string[100];
	int i;
	printf("Enter your string:\n");
	gets (string);
	for (i = 0; string[i] != 0; i++)
	{
		if (string[i] < 'a')
			string[i] = string[i] + 32;
	}
	printf("\n");
		puts (string);
}

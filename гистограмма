#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define YES 1
#define NO 0
main()
{
	int c, length = 0;
	while ((c = getchar()) != EOF)
	{
		if (c == ' ' || c == '\n')
		{
			for (; length > 0; length--)
				printf("D");
			printf("\n");
		}
		else length++;
	}
}
